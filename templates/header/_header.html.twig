{% block stylessheets %}<link rel="stylesheet" href="{{ asset('css/header.css') }}"> {% endblock %}


<div class="head">
   <a href="{{ path('app_accueil') }}"><img src="{{asset('image/logo.png')}}" alt="logo" width='150px'></a>

   <div class="element2_head">
        {% if  app.user %}
        <nav>
        <ul>
            <img src="{{asset('image/utilisateur.png')}}" width='50px'>
            <li class="dropdown">
                <ul class="sous">
                    <li><a href="#"><button class='button_auth'>Mes informations</button></a></li>
                    <li><a href="#"><button class='button_auth'>Mes opérations</button></a></li>
                    <li><a href="{{ path('app_logout') }}"><button class='button_auth'>Se déconnecter</button></a></li>
                </ul>            
            </li>
        </ul>
        </nav>
        {% else %}
        <nav>
        <ul>
            <li><a href="{{ path('app_login') }}"><button class='button_auth'>Se connecter</button></a></li>
        </ul>
        </nav>
        {% endif %}
    </div>
</div>

   

    {# {% if is_granted('EXPERT') %}
    <p>L'administrateur est un expert.</p>
{% else %}
    <p>L'administrateur n'est pas un expert.</p>
{% endif %} #}

   
