{% block stylessheets %}<link rel="stylesheet" href="{{ asset('css/header.css') }}"> {% endblock %}


<div class="head">
   <a href="{{ path('app_accueil') }}"><img src="{{asset('image/logo.png')}}" alt="logo" width='150px'></a>

   <div class="element2_head">
        {% if  app.user %}
        <nav>
        <ul>
            <li class="deroulant"><img src="{{asset('image/utilisateur.png')}}" width='50px'>
                <ul class="sous">
                    <li><a href="#"><button id='button_auth'>Mon compte</button></a></li>
                    <li><a href="{{ path('app_logout') }}"><button id='button_auth'>Se déconnecter</button></a></li>
                </ul>            
        </ul>
        </li>
        </nav>
        {% else %}
        <nav>
        <ul>
            <li><a href="{{ path('app_login') }}"><button id='button_auth'>Se connecter</button></a></li>
        </ul>
        </nav>
        {% endif %}
    </div>
</div>
   

    {# {% if is_granted('EXPERT') %}
    <p>L'administrateur est un expert.</p>
{% else %}
    <p>L'administrateur n'est pas un expert.</p>
{% endif %} #}

   
